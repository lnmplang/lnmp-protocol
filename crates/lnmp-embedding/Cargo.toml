[package]
name = "lnmp-embedding"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
byteorder = "1.4"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "embedding_bench"
harness = false
