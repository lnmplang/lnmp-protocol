# LNMP CLI

Utilities and CLI tools for working with LNMP. This is a placeholder; add
commands and a CLI tool as necessary.

Quickstart (Rust CLI):

```bash
cargo build -p lnmp-cli
cargo run -p lnmp-cli -- --help
```
