# LNMP SDK - Go

This repository holds the Go SDK for LNMP (LLM Native Minimal Protocol).

Quickstart:

```bash
go test ./...
```

Local development:

- Ensure the `lnmp` repo is published (or use `replace` directive in `go.mod` for local testing).
